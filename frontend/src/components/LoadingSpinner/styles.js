import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    circularProgress: {
        width: '100px',
        height: '100px',
        margin: 'auto',
        display: 'block'
    }
});
