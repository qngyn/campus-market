import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    image: {
        width: '100%',
        height: '100%',
    },
    addToCartButton: {
        color: 'white',
        backgroundColor: 'black'
    }
});
